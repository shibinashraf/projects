#include<stdio.h>
struct euclid 
{
int distance;
}dis1,dis2;

void main()
{
int sum=0;

printf("\nEnter the distance between first two points:");
scanf("%d",&dis1.distance);
printf("\nEnter the distance between another two points:");
scanf("%d",&dis2.distance);

sum=dis1.distance+dis2.distance;

printf("\nTotal distance = %d \n",sum);

}